{{- include "artifact-server.validation" . -}}
apiVersion: v1
kind: Pod
metadata:
  name: {{ include "artifact-server.fullname" . }}-test-ready
  labels:
    {{- include "artifact-server.labels" . | nindent 4 }}
    app.kubernetes.io/component: chart-test
  annotations:
    helm.sh/hook: test
    helm.sh/hook-delete-policy: before-hook-creation,hook-succeeded
spec:
  restartPolicy: Never
  # Kubernetes injects legacy Docker-style service-link variables for every
  # Service in the namespace. The Service here is named "artifact-server",
  # so that produces ARTIFACT_SERVER_PORT=tcp://<clusterIP>:80, which
  # collides with Artifact Server's own ARTIFACT_SERVER_PORT setting and
  # makes any command that does not pass --port explicitly fail with
  # "The port must be an integer between 0 and 65535."
  enableServiceLinks: false
  automountServiceAccountToken: false
  securityContext:
    {{- toYaml .Values.podSecurityContext | nindent 4 }}
  containers:
    - name: ready
      image: {{ include "artifact-server.image" . | quote }}
      imagePullPolicy: {{ .Values.image.pullPolicy }}
      command:
        - node
        - -e
        - >-
          fetch('http://{{ include "artifact-server.fullname" . }}/ready')
          .then(async response => {
            if (!response.ok) throw new Error(`readiness returned ${response.status}: ${await response.text()}`);
          })
          .catch(error => { console.error(error); process.exit(1); });
      resources:
        requests:
          cpu: 10m
          memory: 32Mi
      securityContext:
        {{- toYaml .Values.securityContext | nindent 8 }}
      volumeMounts:
        - name: temporary
          mountPath: /tmp
  volumes:
    - name: temporary
      emptyDir:
        medium: Memory
        sizeLimit: 16Mi
